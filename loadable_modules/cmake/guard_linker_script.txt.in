
PHDRS {
    guard_phdr PT_LOAD;
}

SECTIONS {
    .${LIBNAME}.guard : ALIGN(4096) {
        _guard_section_start = ABSOLUTE(.);
        *(.*)
        _guard_section_end = ABSOLUTE(.);
    } >HPSRAM_seg AT>IMR_seg : guard_phdr
}
