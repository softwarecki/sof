# sof Development Guidelines

Auto-generated from all feature plans. Last updated: 2026-04-16

## Active Technologies

- C using the repository's existing firmware toolchain configuration + Existing SOF firmware code only: `source_api.h`, `sink_api.h`, `module_adapter`, `sink_source_utils`, legacy `audio_stream` and `comp_buffer` code used as migration references only (20211020-sink-source-api-extension)

## Project Structure

```text
src/
tests/
```

## Commands

# Add commands for C using the repository's existing firmware toolchain configuration

## Code Style

C using the repository's existing firmware toolchain configuration: Follow standard conventions

## Recent Changes

- 20211020-sink-source-api-extension: Added C using the repository's existing firmware toolchain configuration + Existing SOF firmware code only: `source_api.h`, `sink_api.h`, `module_adapter`, `sink_source_utils`, legacy `audio_stream` and `comp_buffer` code used as migration references only

<!-- MANUAL ADDITIONS START -->
<!-- MANUAL ADDITIONS END -->
